attrs==17.4.0
funcsigs==1.0.2
keyboard==0.11.0
mouse==0.7.0
Pillow==5.0.0
playsound==1.2.2
pluggy==0.6.0
py==1.5.2
# PyAutoGUI==0.9.36  # it doesn't work on travis without gui so install it by hand
# PyMsgBox==1.0.6  # it doesn't work on travis without gui so install it by hand
# PyScreeze==0.1.13  # it doesn't work on travis without gui so install it by hand
# PyTweening==1.0.3  # it doesn't work on travis without gui so install it by hand
pytest==3.4.0
pytest-cov==2.5.1
python-coveralls==2.9.1
pytest-pythonpath==0.7.1
six==1.11.0
